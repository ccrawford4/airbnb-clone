<h1>AirBnb Clone</h1>

<%= link_to 'Add New Rental', new_rental_path, class: 'btn btn-primary' %>

<div class="rental-gallery">
    <% @rentals.each do |rental| %>
        <%= render 'rentals/rental_card', rental: rental %>
    <% end %>
</div>
